services:
  mysql:
    image: mysql:8.4
    container_name: msusers-mysql
    environment:
      MYSQL_ROOT_PASSWORD: ${DB_ROOT_PASSWORD:-aF3MK0ZuWMHHXyX1ZwWjmKoS4baBAUgL}
      MYSQL_DATABASE: ${DB_NAME:-usersdb}
      MYSQL_USER: ${DB_USER:-msuser}
      MYSQL_PASSWORD: ${DB_PASSWORD:-UsrApp2025SecurePass}
    ports:
      - "${DB_PORT:-3307}:3306"
    volumes:
      - msusers_mysql:/var/lib/mysql
      - ./init-users-db.sql:/docker-entrypoint-initdb.d/01-init-users.sql:ro
    networks:
      - default
      - accessibility-shared
    healthcheck:
      test:
        [
          "CMD",
          "mysqladmin",
          "ping",
          "-h",
          "localhost",
          "-p${DB_ROOT_PASSWORD:-aF3MK0ZuWMHHXyX1ZwWjmKoS4baBAUgL}",
        ]
      interval: 10s
      timeout: 5s
      retries: 10

  api:
    image: msusers-api:latest
    build:
      context: .
      dockerfile: ./Dockerfile
    container_name: msusers-api
    depends_on:
      mysql:
        condition: service_healthy
    environment:
      ASPNETCORE_ENVIRONMENT: ${ASPNETCORE_ENVIRONMENT:-Development}
      ConnectionStrings__Default: "server=mysql;port=3306;database=${DB_NAME:-usersdb};user=${DB_USER:-msuser};password=${DB_PASSWORD:-UsrApp2025SecurePass};TreatTinyAsBoolean=false"
      JwtSettings__SecretKey: ${JwtSettings__SecretKey:-9b3e7ER@S^glvxPWKX8nN?DTqtrd%Yj!oVIfh+BG&piHwZz6ky4Q52MumOFA-Lc0}
      JwtSettings__Issuer: ${JwtSettings__Issuer:-https://api.accessibility.company.com}
      JwtSettings__Audience: ${JwtSettings__Audience:-https://accessibility.company.com}
      JwtSettings__ExpiryHours: ${JwtSettings__ExpiryHours:-24}
      Gateway__Secret: ${GATEWAY_SECRET:-VGhpc0lzQVNlY3JldEtleUZvckdhdGV3YXkyMDI0}
    ports:
      - "${API_HOST_PORT:-8081}:8081"
    networks:
      - default
      - accessibility-shared
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8081/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
    labels:
      - "prometheus.scrape=true"
      - "prometheus.port=8081"
      - "prometheus.path=/metrics"
      - "service.name=users-api"
      - "service.version=1.0"

volumes:
  msusers_mysql:

networks:
  default:
    name: accessibility-ms-users_default
  accessibility-shared:
    external: true
    name: accessibility-shared
# Para usar un archivo de entorno específico:
#   docker compose --env-file .env.development up
#   docker compose --env-file .env.production up
